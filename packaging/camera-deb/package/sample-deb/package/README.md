# package

debパッケージへ追加で含める固定ファイルを配置するためのディレクトリです。

現時点では、`build-deb.sh` が必要なパッケージ構成を
`artifacts/deb`配下へ動的に作成するため、このディレクトリは使用していません。

今後、次のような固定ファイルをパッケージへ含める場合に使用します。

- 設定ファイル
- udevルール
- 既定データ
- 補助スクリプト
- systemd以外の固定ファイル

空ディレクトリをGitで管理するため、`.gitkeep`を配置しています。